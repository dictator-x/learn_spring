server:
  port: 3355

spring:
  application:
    name: spring-cloud-config-client
  cloud:
    config:
      label: master
      name: config
      profile: dev
      uri: http://localhost:3344

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7000/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"
